#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <iomanip>
#include <math.h>
#include <algorithm>

#define MAX 100
using namespace std;

std::ifstream input("input.txt");
std::ofstream output("output.txt");

int n, p;
int a[MAX];

int find_min(int i){
    int min_index=i;
    for(int j=i;j<n;j++){
        if ( a[j] < a[min_index]) min_index=j;
    }
    return min_index;
}
void swap(int &a, int &b){
    int temp= a;
    a=b;
    b=temp;
}
void solution() {
    for(int i=0;i<n;i++){
         int min_index=find_min(i);
        swap(a[i],a[min_index]);
    }
     for(int i=0;i<n;i++){
         output<<a[i]<<" ";
    }

}


int main(){
    std::string line;
    input>>n;
      for(int i=0;i<n;i++){
        input>>a[i];
      }
     solution();
    
    
  

    
      
      
      
} 