#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <iomanip>
#include <math.h>
#define MAX 100
using namespace std;

std::ifstream input("input.txt");
std::ofstream output("output.txt");
int n,x;
int a[MAX];
bool checkSNT(int x){
    for(int i=2;i<sqrt(n);i++){
        if(x%i==0) return 0;
    }
    return 1;
}
void tim_day_nguyento(){
    for(int i=0;i<n;i++){
        if(checkSNT(a[i])==1) output<<a[i]<<" ";
    }
}
void solution(){
   tim_day_nguyento();
}

int main(){
    std::string line;
    input>>n;
      for(int i=0;i<n;i++){
        input>>a[i];
      }
    solution();

    
      
      
      
}