#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <iomanip>
#include <math.h>
#include <algorithm>

#define MAX 100
using namespace std;

std::ifstream input("input.txt");
std::ofstream output("output.txt");

int n, p;
int a[MAX];
void swap(int &a, int &b){
    int temp= a;
    a=b;
    b=temp;
}


void solution() {
    bool swapped = true;
    while(swapped==true){
        swapped = false;
        for(int i=0;i<=n-2;i++){
            if(a[i]>a[i+1]) {
                swap(a[i],a[i+1]);
               
                swapped=true;
            }
        }
    }


}


int main(){
    std::string line;
    input>>n;
      for(int i=0;i<n;i++){
        input>>a[i];
      }
     solution();
    for(int i=0;i<n;i++){
         output<<a[i]<<" ";
    }
    
  

    
      
      
      
} 