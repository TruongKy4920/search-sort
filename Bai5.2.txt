#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <iomanip>
#include <math.h>
#define MAX 100
using namespace std;

std::ifstream input("input.txt");
std::ofstream output("output.txt");
int n,x;
int a[MAX];
bool checkSquare(int x){
    int cp= sqrt(x);
    return (pow(cp,2)==x);
}
void Bai5_2(){
    for(int i =0;i<n;i++){
        if(checkSquare(5*a[i]*a[i]+4) || checkSquare(5*a[i]*a[i]-4) ) 
        output<<a[i]<<" ";
    }
}
void solution(){
    Bai5_2();
}

int main(){
    std::string line;
    input>>n;
      for(int i=0;i<n;i++){
        input>>a[i];
      }
    solution();

    
      
      
      
} 