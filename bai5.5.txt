#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <iomanip>
#include <math.h>
#define MAX 100
using namespace std;

std::ifstream input("input.txt");
std::ofstream output("output.txt");

int a[MAX];
int n,c;

int solution() {
   for(int i=0;i<n-1;i++){
    for(int j=i+1;j<=n-1;j++){
        if(a[i]==a[j]) c++;
    }
   }
   return c;
}

int main(){
    std::string line;
    input>>n;
      for(int i=0;i<n;i++){
        input>>a[i];
      }
    
    
    output<<solution();

    
      
      
      
} 